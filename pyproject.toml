[tool.black]
line-length = 100

[tool.mypy]
follow_imports = "normal"
ignore_missing_imports = true
show_error_codes = true
files = "**/*.py"
exclude = [
    '^docs/.*',
    '^build/.*',
    '^venv/.*',
    '^notebooks/.*',
    '^src/my_nb_color.py',
]

[tool.bandit]
skips = ["B101"]
